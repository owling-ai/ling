# 复制为 .env 后按需填写，启动时自动读取（shell 里已有的环境变量优先）。
# 什么都不填 = 规则引擎兜底，全流程照跑。

# ---- 1️⃣ OpenAI 兼容端点（优先级最高：本地 MiniCPM-o / OpenRouter / DeepSeek ...）
# 本地 vLLM-omni 起 MiniCPM-o 4.5：
# LING_OPENAI_BASE_URL=http://localhost:8001/v1
# LING_OPENAI_MODEL=openbmb/MiniCPM-o-4_5

# OpenRouter 走 DeepSeek 示例：
# LING_OPENAI_BASE_URL=https://openrouter.ai/api/v1
# LING_OPENAI_API_KEY=sk-or-...
# LING_OPENAI_MODEL=deepseek/deepseek-chat

# 摄像头画面输入默认按模型名自动判断（含 minicpm-o/omni/vl/vision 则开启），可强制：
# LING_OPENAI_VISION=1

# ---- 2️⃣ Anthropic（没配 OpenAI 兼容端点时启用）
# ANTHROPIC_API_KEY=sk-ant-...
# LING_CHAT_MODEL=claude-opus-4-8
# LING_WORKER_MODEL=claude-haiku-4-5

# ---- 其他
# 强制指定引擎：openai | anthropic | mock
# LING_PROVIDER=openai
# 数据库位置（默认 data/ling.db）
# LING_DB=data/ling.db
